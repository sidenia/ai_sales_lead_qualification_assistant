version: "3.9"

services:
  sales-assistant-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: sales-assistant-api:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
